describe('LoginComponent', () => {
  xit('should redirect to admin page if user is admin', () => {});

  xit('should redirect to client page if user is client', () => {});

  xit('should redirect to login page if user is not activated', () => {});

  xit('should display a toaster if an error occurs', () => {});
});
