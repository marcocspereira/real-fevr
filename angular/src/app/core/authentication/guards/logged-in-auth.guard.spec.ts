describe('LoggedInAuthGuard', () => {
  describe('canActivate', () => {
    xit('should navigate to login page if user is not logged in', () => {});

    xit('should not navigate to login if user is logged in', () => {});
  });
});
