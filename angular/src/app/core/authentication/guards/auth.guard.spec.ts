describe('AuthGuard', () => {
  describe('canActivate', () => {
    xit('should navigate to required page if user is logged', () => {});

    xit('should navigate to required page if user has valid role', () => {});

    xit('should not naviate to required page if user is not allowed', () => {});
  });

  describe('canLoad', () => {
    xit('should navigate to required page if user is logged', () => {});

    xit('should navigate to required page if user has valid role', () => {});
  });
});
